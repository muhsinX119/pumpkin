<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pumpkin">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_pumpkin2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_pumpkin2"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pumpkin"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name="com.example.pumpkin.ScrollingActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Pumpkin.NoActionBar"
            android:windowSoftInputMode="stateHidden"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.pumpkin.EditRecyclerEntry"
            android:theme="@style/Theme.Pumpkin.NoActionBar"
            android:screenOrientation="portrait"/>
    </application>


</manifest>